{
    "watch": ["src"],
    "ext": "ts,js,json",
    "ignore": ["src/**/*.spec.ts"],
    "exec": "node --inspect -r ts-node/register ./src/api/index.ts",
    "env": {
        "NODE_ENV": "development"
    }
}
